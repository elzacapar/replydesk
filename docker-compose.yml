version: '3.8'

services:
  mongo:
    image: mongo:7
    container_name: replydesk-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - replydesk-net

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: replydesk-backend
    restart: unless-stopped
    ports:
      - "8001:8001"
    env_file:
      - .env
    depends_on:
      - mongo
    networks:
      - replydesk-net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: replydesk-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_BACKEND_URL=http://localhost:8001
    depends_on:
      - backend
    networks:
      - replydesk-net

volumes:
  mongo_data:
    driver: local

networks:
  replydesk-net:
    driver: bridge
